# `tools/` 目录说明

| 目录 | 用途 |
|------|------|
| **`tools/shared/`** | 与具体应用无关的可复用脚本：网页抓取、搜索、LLM 封装、截图等。 |

**Aha! Catcher** 的专用脚本、launchd 示例与说明已放在应用目录内：**`ahacatcher/scripts/`**（见 **`ahacatcher/README.md`**）。

通用约定见项目根目录 **`.cursorrules`**（「`tools/` 与 `ahacatcher/scripts/` 约定」）。
